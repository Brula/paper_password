defmodule PaperPassword do
  @moduledoc """
  Password cards, derived from a seed and never stored.

  Everything that matters is `seed -> grid`, under `PaperPassword.Card`.
  `PaperPasswordWeb.HomeLive` is the whole user interface. See ARCHITECTURE.md.
  """
end
